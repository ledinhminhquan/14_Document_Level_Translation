# Full local/dev install (everything). For a minimal core install use `pip install -e .`
# and add extras: `.[ml]`, `.[parsers]`, `.[api]`, `.[report]`, or `.[all]`.

# -- core (package + dictionary MT + line/regex structure parsers + metrics + tests) --
numpy>=1.23
pyyaml>=6.0
pydantic>=2.0
scikit-learn>=1.2
joblib>=1.3

# -- trainable MT core --
torch>=2.1
transformers>=4.40
datasets>=2.18
accelerate>=0.30
sentencepiece>=0.1.99
sacrebleu>=2.3

# -- optional richer document parsers (the line/regex parsers are the default) --
markdown-it-py>=3.0
beautifulsoup4>=4.12
python-docx>=1.1

# -- serving (API + UI) --
fastapi>=0.110
uvicorn[standard]>=0.29
gradio>=4.0
python-multipart>=0.0.9

# -- reporting (auto report.pdf + slides.pptx) --
reportlab>=4.0
python-pptx>=0.6.23
matplotlib>=3.7

# -- optional --
# anthropic>=0.39          # optional LLM terminology-note brain for the agent
